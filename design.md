# 주간 캘린더 디자인 명세

## 1. 문서 목적

이 문서는 데스크톱 웹에서 사용하는 주간 캘린더 화면의 시각 구조와 스크롤 동작을 정의한다. 원본 디자인의 기준 아트보드는 `1677 × 1024px`이며, 화면의 핵심은 다음 세 영역을 서로 어긋나지 않게 유지하는 것이다.

- 선택한 날짜의 요약을 보여 주는 왼쪽 사이드바
- 시간 레이블을 보여 주는 고정 시간축
- 날짜별 일정이 배치되는 주간 캘린더

원본에서 반복되는 절대 좌표와 소수점 크기는 구현 규칙으로 정규화했다. 별도 표기가 없으면 모든 크기의 단위는 논리 픽셀(`px`)이다.

## 2. 제품 기능 범위

이 화면은 독립된 캘린더가 아니라 기존 할 일·메모·시간 기록을 날짜별로 모아 보는 통합 화면이다. 기능 상태는 다음과 같이 구분한다.

- **현재 지원**: 저장소의 클라이언트와 API에 이미 존재하는 기능
- **화면 확장**: 기존 데이터를 새 주간 캘린더 UI에서 제공해야 하는 기능
- **모델 확장 필요**: 시안에는 표현되어 있지만 현재 데이터만으로 완전히 구현할 수 없는 기능

### 전체 앱 기능

| 영역 | 기능 | 상태 |
| --- | --- | --- |
| 인증 | Google 로그인, 로그인 세션 유지, 로그아웃 | 현재 지원 |
| 공통 | 서버 데이터 새로고침, 로컬 캐시 우선 표시, 오류 후 재시도 | 현재 지원 |
| 내비게이션 | 할 일·메모·시간 워크스페이스 이동 | 현재 지원 |
| 할 일 | 오늘·이번 주·이번 달 범위별 목록 | 현재 지원 |
| 할 일 | 생성, 완료/미완료 전환, 수정, 삭제 | 현재 지원 |
| 할 일 | 카테고리, 마감일, 상세 메모 설정 | 현재 지원 |
| 할 일 | 하위 목표 생성·완료·삭제, 진행 개수 표시 | 현재 지원 |
| 할 일 | 범위별 기본 마감일 자동 입력, 마감 지연 표시 | 현재 지원 |
| 할 일 | 반복 루틴 생성, 요일 선택, 활성/비활성, 삭제 | 현재 지원 |
| 월간 캘린더 | 이전·다음 달, 오늘 이동, 날짜별 마감 일정 | 현재 지원 |
| 월간 캘린더 | 하루 최대 3개 미리보기와 추가 개수, 마감일 없는 목록 | 현재 지원 |
| 메모 | 제목·본문 작성, 수정, 삭제 | 현재 지원 |
| 메모 | 제목·본문·태그 통합 검색, `#태그` 자동 추출 | 현재 지원 |
| 메모 | `- [ ]` 또는 `todo:` 줄을 오늘 할 일로 자동 생성 | 현재 지원 |
| 집중 타이머 | 집중·짧은 휴식·긴 휴식, 시간 설정, 시작·일시정지·초기화 | 현재 지원 |
| 집중 타이머 | 집중 완료 후 휴식 자동 시작, 4회마다 긴 휴식 | 현재 지원 |
| 시간 기록 | 작업 기록 시작·종료, 경과 시간 표시, 기록 삭제 | 현재 지원 |
| 시간 기록 | 1분 이상 타이머 기록 저장, 순간 메모 저장 | 현재 지원 |
| 주간 플래너 | 이전·이번·다음 주 이동, 주간 총 집중 시간 | 현재 지원 |
| 주간 캘린더 | 7일 × 시간축 배치, 선택 날짜 요약, 종일 일정 | 화면 확장 |
| 주간 캘린더 | 분 단위 카드 위치, 현재 시각선, 고정 헤더와 동기화 스크롤 | 화면 확장 |
| 주간 캘린더 | 다일 일정, 겹치는 일정, 일정 직접 생성·편집 | 모델 확장 필요 |

### 데이터 저장과 동기화

- 할 일, 메모, 루틴의 생성·수정은 먼저 화면에 반영하고 서버 요청이 실패하면 서버 데이터로 복구한다.
- 작업 중인 시간 기록과 타이머 설정은 기기 로컬 저장소에 유지한다.
- 앱 데이터는 로컬 캐시를 먼저 보여 준 뒤 서버 응답으로 갱신한다.
- 수동 새로고침은 현재 화면을 유지한 채 데이터를 다시 불러온다.
- 저장 실패 시 사용자가 수행한 작업과 실패 원인을 알리고 재시도 경로를 제공한다.

## 3. 주간 캘린더 기능 명세

### 보기 전환과 기간 이동

- 상단 `Today`, `Weekly`, `Monthly`로 오늘 목록, 주간 캘린더, 월간 캘린더를 전환한다.
- `Weekly`가 활성 상태일 때 밑줄과 진한 텍스트를 표시한다.
- 이전·다음 화살표는 정확히 7일씩 이동한다.
- 날짜 범위 또는 별도 `이번 주` 동작으로 현재 주에 돌아갈 수 있어야 한다.
- 주의 시작은 월요일, 끝은 일요일로 통일한다.
- 주를 이동해도 사용자가 선택한 요일을 가능한 한 유지하고, 해당 주에 진입하면 같은 요일을 선택한다.

### 날짜 선택

- 날짜 헤더를 누르면 해당 날짜를 선택한다.
- 선택하면 날짜 열 강조와 왼쪽 일일 요약이 동시에 갱신된다.
- 오늘과 선택 날짜는 서로 다른 상태로 관리한다.
- 키보드에서는 좌우 방향키로 날짜를 이동하고 `Enter` 또는 `Space`로 선택한다.

### 일정 표시

주간 캘린더는 여러 데이터 원본을 하나의 표시 모델로 변환한다.

| 데이터 | 캘린더 표현 | 시간 위치 |
| --- | --- | --- |
| `Todo.dueDate`가 있는 할 일 | 해당 날짜의 종일 일정 또는 할 일 배지 | 종일 행 |
| 완료된 할 일 | 취소선과 흐린 배경의 완료 카드 | 원래 위치 유지 |
| `WorkSession` | 시작·종료 시각에 맞춘 시간 카드 | 분 단위 계산 |
| 순간 메모 세션 | 시간표 카드에서 제외하고 일일 요약 메모에 표시 | 기록 시각 기준 |
| 반복 루틴 | 생성된 할 일로 표시 | 생성된 할 일의 날짜 기준 |

- 일정 카드에는 가능한 범위에서 제목, 시작·종료 시각, 완료 상태를 표시한다.
- 짧은 카드에는 제목만, 충분히 높은 카드에는 시간과 제목을 함께 표시한다.
- 하루에 여러 일정이 같은 시간에 겹치면 카드 폭을 나눠 모두 선택 가능하게 표시한다.
- 화면 밖으로 이어지는 일정은 잘린 방향에 연속 표시를 제공한다.
- 자정을 넘는 기록은 시작일과 다음 날 양쪽 시간표에 나누어 보여야 한다. 현재 도메인의 알려진 제한을 수정할 때 회귀 테스트를 추가한다.

### 일정 생성

- `ADD EVENT`를 누르면 일정 생성 시트 또는 대화상자를 연다.
- 기본 날짜는 선택한 날짜, 기본 시작 시각은 현재 시각을 다음 15분 단위로 올림한 값이다.
- 입력 항목은 제목, 날짜, 시작 시각, 종료 시각, 종일 여부, 카테고리, 메모다.
- 종료 시각은 시작 시각보다 뒤여야 한다. 자정 초과 일정은 다음 날 종료로 명시한다.
- 저장 후 생성 창을 닫고 해당 주와 날짜를 유지한 채 새 카드를 즉시 표시한다.
- 취소하면 입력 내용을 저장하지 않는다. 입력 중 닫을 때는 내용이 있으면 폐기 확인을 표시한다.
- 현재 `Todo`와 `WorkSession`만으로는 일반 일정과 종일·다일 일정을 모두 표현할 수 없으므로, 구현 전 `CalendarEvent` 모델 또는 동등한 API 확장이 필요하다.

### 일정 상세·수정·삭제

- 일정 카드를 누르면 상세 보기 또는 편집 시트를 연다.
- 상세 화면에서 제목, 날짜와 시각, 카테고리, 메모, 완료 상태를 확인한다.
- 편집 저장 시 카드 위치와 일일 요약을 즉시 다시 계산한다.
- 삭제 전 일정 제목을 포함한 확인 절차를 제공한다.
- 할 일 완료 체크는 기존 완료 시각과 부모 완료 전파 규칙을 그대로 사용한다.
- 읽기 전용으로 투영한 시간 기록은 상세 보기와 삭제만 제공하고, 시각 편집은 별도 기능으로 명시하기 전에는 제공하지 않는다.

### 종일·다일 일정

- 종일 일정은 시간표가 아닌 고정된 종일 행에 표시한다.
- 다일 일정은 시작일부터 종료일까지 하나의 연속 바로 표시한다.
- 주 경계를 넘는 일정은 현재 주의 첫날 또는 마지막 날에서 잘라 표시하고 연속 표식을 붙인다.
- 같은 행에서 겹치는 일정은 다음 줄에 쌓는다.
- 고정 높이에 표시할 수 없는 일정은 `+N개`로 접고 전체 목록을 열 수 있게 한다.

### 일일 요약

선택 날짜에 대해 다음 정보를 제공한다.

- 총 집중 시간: 해당 날짜와 겹치는 모든 작업 세션의 실제 겹침 시간을 합산
- 완료 일정: 해당 날짜에 완료했거나 해당 날짜 일정 중 완료 상태인 할 일
- 오늘의 메모: 선택 날짜에 작성된 순간 메모
- 빠른 메모: 입력 후 현재 시각의 순간 메모로 저장

완료 일정은 체크 상태와 제목을 표시하고 선택 시 원본 할 일 상세로 이동한다. 순간 메모는 생성 시각 순으로 표시하며 삭제할 수 있다. 선택 날짜에 데이터가 없으면 각 섹션 안에 빈 상태를 표시한다.

### 현재 시각과 자동 위치

- 현재 주를 보고 있을 때만 오늘 열에 현재 시각선을 표시한다.
- 현재 시각선은 1분마다 갱신하되 사용자의 스크롤을 변경하지 않는다.
- 첫 진입은 현재 시각을 세로 중앙 근처에 배치한다.
- 과거 또는 미래 주로 이동하면 `09:00`을 기본 위치로 사용한다.
- `Today`로 돌아오면 오늘 날짜를 선택하고 현재 시각으로 이동한다.

### 로딩·오류·빈 상태

- 첫 로딩은 날짜 열과 카드 크기를 보존하는 스켈레톤을 사용한다.
- 캐시가 있으면 캐시를 즉시 보여 주고 백그라운드 갱신 상태만 작게 표시한다.
- 주간 데이터가 비어 있어도 시간축과 현재 시각선은 유지한다.
- 갱신 실패 시 기존 데이터를 지우지 않고 재시도 메시지를 도구 모음 아래에 표시한다.
- 생성·수정·삭제 실패 시 낙관적 변경을 되돌리고 사용자가 다시 시도할 수 있게 한다.

## 4. 화면 구조

```text
┌──────────────────────────────────────────────────────────────────────┐
│ 상단 내비게이션 · 61                                                 │
├──────────────────────────────────────────────────────────────────────┤
│ 주간 도구 모음 · 56                                                   │
├───────────────┬──────┬───────────────────────────────────────────────┤
│ 일일 요약      │ 시간축 │ 주간 캘린더                                  │
│ 280 · 고정     │ 64   │ 남은 폭 · 가로 스크롤                         │
│               │ 고정  │                                               │
│ 독립 세로      ├──────┼───────────────────────────────────────────────┤
│ 스크롤         │ 시간  │ 시간표 · 시간축과 함께 세로 스크롤            │
└───────────────┴──────┴───────────────────────────────────────────────┘
```

### 최상위 레이아웃

| 영역 | 기준 크기 | 동작 |
| --- | ---: | --- |
| 상단 내비게이션 | `100% × 61` | 화면 상단 고정 |
| 보조 도구 모음 | `100% × 56` | 상단 내비게이션 아래 고정 |
| 메인 콘텐츠 | `100% × calc(100vh - 117px)` | 내부 영역만 스크롤 |
| 일일 요약 | `280 × 100%` | 왼쪽 고정, 내용은 독립 스크롤 |
| 시간축 | `64 × 100%` | 가로 위치 고정 |
| 캘린더 뷰포트 | 남은 너비 | 최소 콘텐츠 폭 이하에서 가로 스크롤 |

페이지 전체가 스크롤되지 않도록 최상위 컨테이너는 `100vh`와 `overflow: hidden`을 사용한다. 작은 높이에서도 헤더는 보이고, 일일 요약과 시간표만 각각 스크롤되어야 한다.

## 5. 상단 내비게이션

높이는 `61`이고 배경은 `#FDFDFD`, 아래 테두리는 `#E5E7EB`이다. 좌우 패딩은 `24`, 세로 패딩은 `16`을 기준으로 한다.

- 왼쪽: `BlankDay` 워드마크
  - Inter, `20/28`, 굵기 `700`, 자간 `-0.5`
  - 색상 `#111827`
- 가운데: `Today`, `Weekly`, `Monthly`
  - Inter, `14/20`, 굵기 `500`
  - 비활성 `#6B7280`, 활성 `#000000`
  - 활성 항목 아래에 `2px` 검정 밑줄
  - 항목 간 간격 `24`
- 오른쪽: 메모와 타이머 빠른 실행 아이콘
  - 아이콘 `20 × 20`, 색상 `#6B7280`
  - 터치 영역은 최소 `36 × 36`으로 확장

## 6. 주간 도구 모음

높이는 `56`, 좌우 패딩은 `15`, 아래 테두리는 `#E5E7EB`이다.

- 왼쪽 그룹
  - 이전 주 버튼 `20 × 20`
  - 날짜 범위: Inter, `18/28`, 굵기 `600`, `#111827`
  - 다음 주 버튼 `20 × 20`
  - 아이콘과 날짜 사이 간격 `12`
- 오른쪽 그룹
  - `+ ADD EVENT` 버튼 `100 × 32`
  - 배경 `#111111`, 모서리 `4`
  - 텍스트 `12/16`, 굵기 `600`, 자간 `0.6`, 대문자

이전/다음 버튼은 주 단위로 이동한다. 날짜 범위는 `YYYY.MM.DD – YYYY.MM.DD` 형식을 기본으로 하되, 제품의 로케일 포맷터를 사용할 수 있다.

## 7. 일일 요약 사이드바

너비 `280`, 배경 `#F4F4F4`, 오른쪽 구분선 `#E5E7EB`이다.

### 고정 헤더

1. 섹션 제목 영역: 높이 `96`
   - `DAILY SUMMARY`
   - `12/16`, 굵기 `600`, 자간 `1.2`, 대문자, `#6B7280`
2. 선택 날짜 영역: 높이 `72`
   - 날짜 텍스트 `18/16`, 굵기 `700`, 자간 `1.2`, `#6B7280`

두 영역에는 각각 아래 구분선을 둔다. 헤더 아래의 내용 영역만 세로로 스크롤한다.

### 스크롤 콘텐츠

콘텐츠 패딩은 `16`, 내부 너비는 `248`이다. 섹션 제목은 `10/15`, 굵기 `700`, 자간 `0.5`, 대문자, `#9CA3AF`를 사용한다.

#### 총 집중 시간

- 상하 패딩 `8`, 제목과 카드 간격 `8`
- 카드 높이 `46`, 흰색 배경, `1px #E5E7EB` 테두리, 모서리 `4`
- 그림자 `0 1px 2px rgba(0, 0, 0, 0.05)`
- 시간 값은 가운데 정렬하며 `20/20`, 굵기 `700`, `#1F2937`

#### 완료 일정

- 위 구분선과 `16` 간격 후 제목 표시
- 항목 높이 `46`, 항목 간격 `8`
- 각 항목은 완료 아이콘 `16`, 제목 `14/20`, `#1F2937`로 구성
- 긴 제목은 한 줄 말줄임 처리하고 전체 제목은 접근성 레이블로 제공

#### 오늘의 메모

- 위 구분선과 `16` 간격 후 제목 표시
- 빠른 메모 입력 카드 높이 `46`, 본문 `12/20`, `#4B5563`
- 메모 목록 카드는 내용에 따라 늘어나며 최소 패딩 `13`
- 본문은 `12/20`, 굵기 `700`, `#4B5563`

요약 데이터는 현재 선택된 날짜를 기준으로 즉시 다시 계산한다. 빈 섹션은 제목을 유지하고 `기록 없음` 상태를 보여 준다.

## 8. 캘린더 상단 고정 영역

캘린더의 날짜 열은 월요일부터 일요일까지 7개다. 날짜 영역의 기준 폭은 `936`이며 각 열은 동일하게 약 `133.7`이다. 실제 뷰포트가 넓으면 열을 균등 확장하고, 좁으면 이 폭을 최소값으로 유지한다.

### 날짜 헤더

- 높이 `96`, 배경 `#FFFFFF`
- 각 열 오른쪽에 `1px #E5E7EB` 구분선
- 내부 패딩: 위·아래 `8`, 좌 `12`, 우 `13`
- 요일: `10/15`, 굵기 `600`, 자간 `0.5`, 대문자
- 큰 날짜: `36/40`, 굵기 `700`
- 작은 날짜 배지: `24 × 24`, `11/16`, 굵기 `700`

날짜 상태는 다음처럼 표현한다.

| 상태 | 표현 |
| --- | --- |
| 기본 | 흰 배경, 큰 날짜 `#000000`, 작은 날짜 배지 `#F3F4F6` |
| 선택됨 | 열 헤더 배경 `#000000`, 큰 날짜 `#FFFFFF` |
| 오늘 | 작은 날짜 배지에 `1px #000000` 외곽선 |
| 비활성/범위 밖 | 큰 날짜 `#D1D5DB`, 요일 `#6B7280` |

선택 상태와 오늘 상태는 독립적이다. 같은 날짜에 겹치면 선택 상태를 우선하고 스크린 리더용 `오늘, 선택됨` 설명을 함께 제공한다.

### 종일·다일 일정 행

- 높이 `72`
- 배경 `rgba(249, 250, 251, 0.5)`
- 아래 구분선 `#E5E7EB`
- 날짜 열 구분선은 시간표 열과 정확히 같은 x 좌표를 사용

다일 일정은 시작일과 종료일에 맞춰 열을 가로질러 배치한다. 바 높이는 `30`, 세로 간격은 `3`, 좌우 여백은 `4`다. 보이는 행 수를 초과하면 `+N개` 버튼으로 접고, 누르면 해당 주의 전체 종일 일정을 표시한다.

기본 색상 예시:

- 파랑: 배경 `#DBEAFE`, 테두리 `#BFDBFE`, 제목 `#1E40AF`, 보조 배지 `#1E3A8A`
- 분홍: 배경 `#FCE7F3`, 테두리 `#FBCFE8`, 제목·보조 배지 `#9D174D`

## 9. 시간표

### 시간 단위와 그리드

- 한 시간 높이: `57.5`
- 전체 24시간 콘텐츠 높이: `1380`
- 시간 레이블: `10/15`, 굵기 `500`, `#9CA3AF`
- 정오처럼 강조할 레이블: `#6B7280`
- 시간선: `1px rgba(229, 231, 235, 0.5)`
- 주요 경계선: `2px #E5E7EB`
- 날짜 열 구분선: `1px #E5E7EB`
- 선택된 날짜 열 배경: `#F9FAFB`

시간 레이블은 `9 AM`, `10 AM`, `12 PM` 형식으로 표시한다. 시간선, 시간 레이블, 일정의 위치는 모두 동일한 `hourHeight` 상수를 사용해 계산한다.

```text
top = minutesFromStartOfDay / 60 × hourHeight
height = durationInMinutes / 60 × hourHeight
left = dayIndex × dayWidth
width = daySpan × dayWidth
```

일정의 시각이 정확한 정시가 아니어도 분 단위 위치를 유지한다. 최소 표시 높이는 `24`로 하되 실제 시작·종료 시각은 텍스트와 접근성 레이블에 보존한다.

### 일정 카드

- 날짜 열 안쪽 여백 `4`
- 기본 배경 `#FFFFFF`
- 테두리 `1px #D1D5DB`, 모서리 `2`
- 그림자 `0 1px 2px rgba(0, 0, 0, 0.05)`
- 기본 텍스트 `12/16`, `#1F2937`
- 작은 카드 텍스트 `10/12`
- 완료 일정은 배경 `#F3F4F6`에 `0.6` 투명도, 제목에 취소선
- 강조/선택 일정은 검정 배경과 흰 텍스트

체크 가능한 일정은 왼쪽에 `16 × 16` 체크박스를 둔다. 카드가 충분히 높으면 시간과 제목을 두 줄로, 작으면 제목만 한 줄 말줄임으로 표시한다. 같은 시간대의 일정이 겹치면 열 내부에서 폭을 나눠 배치한다.

### 현재 시각 표시선

- 오늘 열에만 표시
- 높이 `1`, 색상 `#000000`
- 시작점에 지름 `12`의 원형 마커
- 현재 시각이 바뀌면 최소 1분마다 위치 갱신

## 10. 스크롤 동작

스크롤은 이 화면의 핵심 상호작용이다.

### 세로 스크롤

- 날짜 헤더와 종일 일정 행은 고정한다.
- 시간 레이블 열과 일정 그리드는 하나의 세로 스크롤 위치를 공유한다.
- 어느 쪽에서 휠, 트랙패드, 터치 드래그가 시작되어도 두 영역이 함께 이동한다.
- 일일 요약 내용은 시간표와 무관한 별도 세로 스크롤 컨테이너다.
- 최초 진입 시 오늘의 현재 시각이 화면 중앙 부근에 오도록 스크롤한다. 현재 시각이 없거나 다른 주를 보는 경우 `09:00`으로 이동한다.
- 사용자가 스크롤한 뒤 데이터가 갱신되어도 위치를 임의로 초기화하지 않는다.

### 가로 스크롤

- 일일 요약과 시간축은 가로 스크롤에 참여하지 않는다.
- 날짜 헤더, 종일 일정 행, 시간표는 하나의 가로 오프셋을 공유한다.
- 캘린더 콘텐츠의 최소 폭은 `936`이다.
- 뷰포트가 최소 폭보다 넓으면 7개 날짜 열이 남은 너비를 균등하게 채운다.
- 뷰포트가 좁으면 날짜 열의 너비를 줄이지 않고 가로 스크롤을 제공한다.

### 스크롤바와 경계

- 데스크톱 웹에서는 세로·가로 스크롤바를 사용할 수 있어야 한다.
- 스크롤바가 날짜 열 너비 계산을 바꾸거나 헤더와 본문을 어긋나게 해서는 안 된다.
- 스크롤 끝에서 상위 페이지로 휠 이벤트가 전달되어 페이지 전체가 움직이는 현상을 막는다.
- React Native 구현에서는 중첩 `ScrollView`의 동일 축 충돌을 피하고, 가능하면 헤더와 본문을 같은 가로 스크롤 컨테이너에 둔다. 분리해야 한다면 단일 오프셋을 기준으로 동기화한다.

## 11. 반응형 규칙

이 명세의 완전한 3열 레이아웃은 웹 너비 `1100` 이상을 기준으로 한다.

| 화면 폭 | 동작 |
| --- | --- |
| `≥ 1440` | 일일 요약 `280`, 시간축 `64`, 날짜 열은 남은 폭에 맞춰 확장 |
| `1100–1439` | 일일 요약과 시간축 유지, 캘린더에 가로 스크롤 허용 |
| `< 1100` | 일일 요약을 접이식 패널/시트로 이동하고 시간축과 캘린더를 우선 표시 |
| `< 768` | 하루 또는 3일 보기로 전환하는 별도 모바일 레이아웃 사용 |

모바일에서 데스크톱 주간표를 단순 축소하지 않는다. 터치 대상은 최소 `44 × 44`를 보장한다.

## 12. 디자인 토큰

### 색상

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `color.text.primary` | `#111827` | 제목, 주요 날짜 |
| `color.text.body` | `#1F2937` | 본문, 일정 제목 |
| `color.text.secondary` | `#6B7280` | 내비게이션, 보조 텍스트 |
| `color.text.muted` | `#9CA3AF` | 메타 정보, 시간 레이블 |
| `color.border` | `#E5E7EB` | 기본 구분선 |
| `color.surface` | `#FFFFFF` | 카드와 캘린더 배경 |
| `color.surface.subtle` | `#F9FAFB` | 선택 열, 보조 배경 |
| `color.sidebar` | `#F4F4F4` | 일일 요약 배경 |
| `color.inverse` | `#000000` | 선택 상태, 강조 버튼 |

### 타이포그래피와 형태

- 기본 글꼴: `Inter`; 사용할 수 없으면 시스템 sans-serif
- 숫자 시간과 날짜에는 `tabular-nums` 사용
- 기본 카드 모서리: `4`
- 일정 모서리: `2`
- 원형 날짜 배지: `9999`
- 기본 그림자: `0 1px 2px rgba(0, 0, 0, 0.05)`

## 13. 접근성 및 상호작용 상태

- 아이콘만 있는 버튼에는 `accessibilityLabel`을 제공한다.
- 요일과 날짜는 축약된 시각 텍스트와 별개로 완전한 날짜를 읽어 준다.
- 선택, 오늘, 완료 상태는 색상만으로 구분하지 않는다.
- 키보드로 주 이동, 날짜 선택, 일정 선택, 스크롤 영역 진입이 가능해야 한다.
- 포커스 링은 `2px` 이상이며 배경과 `3:1` 이상의 대비를 갖는다.
- 본문 텍스트는 가능한 한 WCAG AA 대비를 만족한다.
- 로딩 중에는 기존 레이아웃 크기를 유지하고 스켈레톤을 표시한다.
- 오류 발생 시 시간표를 지우지 않고 도구 모음 아래에 재시도 가능한 오류 메시지를 표시한다.

## 14. 권장 컴포넌트 구조

```text
WeeklyCalendarScreen
├─ TopNavigation
├─ WeeklyToolbar
└─ CalendarWorkspace
   ├─ DailySummary
   │  ├─ SummaryHeader
   │  └─ SummaryScrollContent
   ├─ TimeAxis
   │  ├─ TimeAxisSpacer
   │  └─ TimeLabels
   └─ CalendarViewport
      ├─ WeekdayHeader
      ├─ MultiDayEventRow
      └─ Timetable
         ├─ GridBackground
         ├─ TimedEventLayer
         └─ CurrentTimeIndicator
```

날짜 계산, 주 경계, 일정의 `top/height/dayIndex` 계산은 화면 컴포넌트에 인라인하지 않고 `client/src/**/domain/`의 순수 함수로 둔다. 현재 시각은 함수 내부에서 생성하지 않고 인자로 전달한다.

## 15. 완료 조건

### 기능

- 이전·다음·이번 주 이동과 날짜 선택이 정확한 로컬 날짜 경계에서 동작한다.
- 할 일, 시간 기록, 순간 메모가 명세의 표시 모델에 따라 올바른 날짜와 영역에 나타난다.
- 일정 생성·수정·삭제 후 현재 주와 스크롤 위치를 유지하며 화면이 즉시 갱신된다.
- 선택 날짜의 총 집중 시간, 완료 일정, 메모가 원본 데이터와 일치한다.
- 종일·다일·자정 초과·겹치는 일정이 정보 손실 없이 표시된다.
- 로딩, 빈 데이터, 네트워크 실패, 저장 실패 상태에서 사용자가 다음 행동을 선택할 수 있다.

### 레이아웃과 스크롤

- 기준 화면 `1677 × 1024`에서 상단 `117`을 제외한 영역이 내부 스크롤로 채워진다.
- 일일 요약 `280`과 시간축 `64`는 캘린더 가로 스크롤 중에도 움직이지 않는다.
- 날짜 헤더, 종일 일정, 시간표의 7개 열 경계가 모든 스크롤 위치에서 일치한다.
- 시간 레이블과 일정 그리드가 세로 스크롤 중 한 프레임 이상 어긋나지 않는다.
- 24시간 전체에 접근할 수 있고 최초 위치는 현재 시각 또는 `09:00`이다.
- 일정 위치와 높이가 시작·종료 분을 반영한다.
- 선택 날짜가 바뀌면 일일 요약과 선택 열 표시가 함께 갱신된다.
- 키보드와 스크린 리더로 주요 내비게이션과 일정에 접근할 수 있다.
- `1100` 이하에서 캘린더 내용이 잘리지 않고 명시된 축소 레이아웃으로 전환된다.
