{
  "name": "free-adhd-memo-client",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html sw.js manifest.webmanifest dist/ && cp -R src dist/src && cp -R icons dist/icons"
  }
}
