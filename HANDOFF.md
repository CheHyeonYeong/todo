# 인수인계: 클라이언트 리팩토링

> 진행 중인 작업의 현재 상태다. **두 PR이 머지되면 이 파일은 지운다.**
> 저장소 전반의 규칙은 `AGENTS.md`에 있다.

## 열려 있는 PR

머지 순서는 **#44 → #45**다. #44가 머지되면 #45의 base가 자동으로 main으로 바뀐다.

| PR | 브랜치 | 내용 |
| --- | --- | --- |
| [#44](https://github.com/CheHyeonYeong/todo/pull/44) | `refactor/client-01-ci` | CI 구축, 도메인 순수함수 추출, 버그 수정 5건 |
| [#45](https://github.com/CheHyeonYeong/todo/pull/45) | `refactor/client-02-readable` | prettier 포맷, 이 문서 |

두 PR 모두 CI 통과 상태다(서버 테스트 28, 클라이언트 테스트 60, 타입 체크, 포맷, Vercel 빌드).

## 무엇을 왜 했나

Expo 이관 과정에서 비즈니스 규칙이 렌더 코드 안에 녹아버린 상태였다.
`Screens.tsx`가 174줄인데 한 줄이 최대 2,389자였고, 그 안에 컴포넌트 하나가 통째로 들어 있었다.
`new Date()`가 로직 곳곳에서 43번 불렸고, 클라이언트 테스트는 0개였다.

레이어를 나누거나 폴더 구조를 갈아엎지 않았다. **규칙을 순수함수로 꺼내고 테스트를 붙였을 뿐이다.**
그 과정에서 버그 5개가 드러났다 — 리팩토링의 성과라기보다 안 보이던 게 보이게 된 것이다.

1. 달력의 "오늘" 표시가 UTC라 KST 기준 하루 아홉 시간 동안 어제에 찍혔다
2. 태그 정규식이 구두점을 삼켰다 (`#작업,`)
3. 메모를 수정해도 태그가 갱신되지 않았다 — 클라이언트도 서버도 재계산하지 않았다
4. `sortOrder`를 클로저에서 계산해 다른 기기가 추가한 할 일을 보지 못했다
5. 4번을 고치면서 서버가 정한 순서를 클라이언트가 배우지 않는 틈이 생겼다

각 수정의 배경은 커밋 메시지에 적혀 있다. `git log --format='%s%n%n%b' main..refactor/client-02-readable`

## 이어서 할 만한 것

우선순위 순.

1. **코드 리뷰가 끝내 실행되지 않았다.** 사용 한도에 걸려 에이전트가 diff를 받기 전에 종료됐다.
   #44는 독립적인 리뷰를 한 번도 받지 않은 상태다.
2. **통합 테스트 복구.** `tests/e2e.mjs`가 Expo 재작성 때 삭제돼 지금은 Vercel 빌드가 유일한 통합 검증이다.
3. `AGENTS.md`의 "알려진 문제" 항목들.

## 하지 말 것

- **`domain/`에 `new Date()`를 넣지 않는다.** "지금"은 인자로 받는다. 이걸 어기면 폴더의 존재 이유가 사라진다.
- **동작 변경과 포맷·이동을 한 커밋에 섞지 않는다.** #44가 커밋 8개로 나뉜 이유가 이것이다.
- **DDD 레이어링·FSD를 도입하지 않는다.** 검토했고 이 규모에서는 손해라고 판단했다.
  서버(`server/`)는 이미 바운디드 컨텍스트로 나뉘어 있지만, 클라이언트는 504줄짜리라 사정이 다르다.
